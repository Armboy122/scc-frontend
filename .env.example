# SCC Frontend env template
# Copy to .env.local for local overrides. Never put secrets in NEXT_PUBLIC_* values.
# Production public defaults live in tracked .env.production; Vercel may override them per environment.
NEXT_PUBLIC_API_BASE_URL=https://api.103.117.151.158.sslip.io/api/v1

# Safe local defaults keep unfinished rollout surfaces disabled.
NEXT_PUBLIC_ENABLE_PHASE2_BORROWING=false
NEXT_PUBLIC_ENABLE_PHASE3_EXPANSION=false
